# 投聊（touliao）全项目审查指令清单

**使用方法**

1. 开始前先在项目里 `git init` 并提交一次，保证能回滚
2. **按顺序一条一条发给 Claude，不要一次全发**
3. 每条跑完，先看它的报告，你确认后再用文末的「修复指令」让它改
4. 每轮修改后自己跑一遍主流程，再 commit

**项目背景（每条指令开头可复用）**

> 这是我的社交 App「投聊」（touliao）。技术栈：Node.js 单体后端（Express + SQLite wechat.db + WebSocket，PM2 进程名 touliao-backend，端口 3003）、React Web（Vite build → nginx /var/www/touliao-web，Electron 包壳 desktop-electron/）、Kotlin Compose 原生 Android、SwiftUI 原生 iOS。单机 1000 人私有化部署。

---

## 第 0 条｜摸底（只读不改）

```
这是我的社交 App「投聊」（touliao）。技术栈：Node.js 单体后端（Express + SQLite wechat.db + WebSocket，PM2 进程名 touliao-backend，端口 3003）、React Web（Vite build → nginx /var/www/touliao-web，Electron 包壳 desktop-electron/）、Kotlin Compose 原生 Android、SwiftUI 原生 iOS。单机 1000 人私有化部署。

请先只读不改：
1. 输出后端路由清单（method + path + 是否鉴权 + 对应 handler 文件），以及 WebSocket 事件清单。
2. 输出 SQLite 完整表结构、索引、外键约束。
3. 列出四端各自已实现的功能模块，以及 TODO/mock/写了一半的地方。
4. 对比 Web / Android / iOS 三端的功能覆盖差异，列出「某端有、某端没有」的功能。
5. 全部写入根目录 AUDIT.md。
这一步不要修改任何代码。
```

---

## 第 1 条｜认证与会话（四端一致性）

```
只审查投聊的认证与会话，不改代码，报告问题文件和行号：
- 密码是否 bcrypt/argon2 加盐，有无 md5/sha1/明文
- JWT secret 是否硬编码在仓库里，有无 .env 泄露到 git
- token 过期、刷新机制；登出后是否真正失效；改密码后旧 token 是否失效
- 登录接口限流、验证码有效期与错误次数限制
- 登录失败提示是否区分「账号不存在」和「密码错误」
- token 存储：Web 用 localStorage 还是 httpOnly cookie；Electron 是否暴露在渲染进程；Android 是否用 EncryptedSharedPreferences；iOS 是否用 Keychain（不能用 UserDefaults）
- WebSocket 握手时是否做鉴权，还是连上就能收发（重点查这条）
```

---

## 第 2 条｜WebSocket 实时链路（本架构最脆弱的部分）

```
只审查投聊的 core/realtime 与后端 WebSocket，不改代码：
- 连接鉴权：握手时校验 token 吗？token 过期后连接是否被断开？
- 消息投递：客户端发出的消息有无本地 msgId + 服务端 ack，断线重连后未 ack 的消息是否重发
- 消息去重：重发会不会导致对方收到两条（有无幂等 msgId 唯一索引）
- 消息顺序：并发发送时序号如何保证，客户端如何排序
- 离线消息：用户不在线时消息是否落库，上线后如何拉取增量（全量拉还是按 lastMsgId）
- 心跳与重连：有无 ping/pong、超时断开、指数退避重连；Android 后台被杀、iOS 进后台、Electron 休眠唤醒后的重连表现
- 广播是否越权：能否通过伪造 roomId/conversationId 收到别人的会话消息
- 单机 1000 人：连接对象是否有内存泄漏（断开后是否从 map 中清理），有无连接数上限
输出问题表格，按严重程度排序。
```

---

## 第 3 条｜越权与鉴权

```
只审查投聊的接口鉴权，不改代码，输出表格：接口 / 是否鉴权 / 是否校验资源归属 / 风险等级。
重点：
- 水平越权：改 userId / postId / conversationId 能否读改别人的资料、帖子、私信
- 垂直越权：普通用户能否调管理员接口
- 删除/编辑操作是否校验归属者
- 拉黑、私密账号、仅好友可见是否在后端真正生效，还是只在前端隐藏
- 上传的文件 URL 是否可被未授权者直接访问（nginx 静态目录是否裸奔）
```

---

## 第 4 条｜SQLite 专项（单点瓶颈）

```
只审查投聊的数据库层，不改代码：
- 是否开启 WAL 模式（journal_mode=WAL），未开启会导致读写互相阻塞
- 是否设置 busy_timeout，高并发下有无 SQLITE_BUSY / database is locked 风险
- 是否有连接池或串行化写入，多个请求同时写会怎样
- 消息表、feed 表是否建了必要索引（conversationId+createdAt、userId+createdAt 等），有无全表扫描
- 是否使用参数化查询，有无字符串拼接 SQL
- 是否有事务缺失导致的数据不一致（发消息 + 更新会话最后一条 + 未读数是否在一个事务里）
- 分页是否用 offset（数据变动会重复/漏），消息列表建议改游标分页
- wechat.db 文件权限、备份策略、是否会无限增长（消息/日志清理策略）
- 删用户后帖子、评论、消息的级联处理
```

---

## 第 5 条｜输入安全

```
只审查投聊的输入处理，不改代码：
- XSS：React 里有无 dangerouslySetInnerHTML；昵称/简介/消息内容输出是否转义
- Electron 安全：nodeIntegration 是否关闭、contextIsolation 是否开启、有无 remote 模块、外链是否用 shell.openExternal 且校验协议
- 上传：后端是否校验真实 MIME 而非扩展名、大小限制、文件名路径穿越、图片是否重编码去 EXIF（GPS 泄露）
- 所有接口参数的类型与长度校验，缺参/错类型/null 会不会 500
- 错误堆栈是否泄露给客户端
- CORS 配置是否过宽（是否 origin: *）
```

---

## 第 6 条｜业务逻辑与并发

```
只审查投聊的业务逻辑，不改代码：
- 点赞/关注/收藏：连点两次会不会产生重复记录，计数会不会算错，有无唯一索引或原子操作
- 取消关注/删帖后，计数、feed、通知里的残留数据是否清理
- 互相拉黑、自己关注自己、给自己发私信等边界情况
- 未读数：多端登录时如何同步，已读回执逻辑是否正确
- 消息撤回：时限校验、撤回后本地和对端是否都清理
- 群聊：退群/踢人/解散后的消息可见性，@ 提醒，群主转让
- 列表为空、最后一页、超大 page 参数的表现
- 删除是软删还是硬删，级联关系是否处理
```

---

## 第 7 条｜前端健壮性（Web / Electron）

```
只审查投聊的 React Web 与 Electron 端，不改代码：
- 所有网络请求是否有 try/catch、超时设置、失败重试和用户可见的错误提示
- 断网、请求超时、服务端返回 500 时页面表现（是不是白屏或一直转圈）
- 加载态、空状态、错误状态三种 UI 是否都实现了
- 长消息列表是否有虚拟滚动或分页，图片是否懒加载和有占位
- 组件卸载后有没有取消请求、清定时器、断开监听（内存泄漏）
- 极端内容：超长昵称、纯 emoji、换行符、右到左文字、超宽图片，布局会不会崩
- Electron 特有：多窗口、托盘、开机自启、自动更新失败回退、休眠唤醒后重连
```

---

## 第 8 条｜三端功能对齐

```
逐项对比投聊的 Web / Android / iOS 三端，输出表格：功能 / Web / Android / iOS / 后端接口是否就绪。
覆盖：注册登录、找回密码、账号注销、资料编辑、发帖（图文/视频）、草稿、编辑删除、信息流、点赞评论、关注粉丝、单聊、群聊、消息撤回、已读回执、正在输入、图片/语音/文件消息、推送（个推/APNs）、通知中心、搜索、举报拉黑、隐私设置、深色模式、多语言。
对「部分实现」要写清缺口。输出到 AUDIT.md。
```

---

## 第 9 条｜移动端专项

```
只审查投聊的移动端，不改代码。

Android（Kotlin + Compose + Coroutines）：
- Coroutine 是否绑定正确的 scope，有无泄漏；ViewModel 里有无 GlobalScope
- Compose 重组性能：有无在 composable 里做耗时操作、不稳定参数导致的过度重组
- 配置变更（旋转）、进程被杀后状态恢复
- 个推：token 上报时机、账号切换后是否解绑旧 token（否则会串号推送，重点查）
- 网络：Retrofit 超时、重试、错误统一处理；断网 UI 表现
- 权限申请（通知、相册、相机）被拒绝后的降级路径

iOS（SwiftUI）：
- @StateObject / @ObservedObject 用法是否正确导致状态丢失
- 进后台后 WebSocket 处理、APNs 推送权限与 token 上报、账号切换解绑
- Keychain 存 token、后台任务、内存增长
```

---

## 第 10 条｜品牌与命名一致性

```
只读不改，检查投聊的品牌一致性：
- 四端 App 显示名、启动页、关于页、版本号是否统一为「投聊」，有无残留的模板名、测试名、其他项目名
- Android applicationId / iOS Bundle ID / Electron appId / package.json name 是否规范统一，有无 com.example 之类的占位
- 数据库文件名是 wechat.db，与项目名不一致 —— 排查是否为早期模板残留，代码里有无其他 "wechat" 命名残留（表名、类名、接口路径、注释）。如果是从微信克隆模板改的，列出所有可能引发商标或合规风险的命名、图标、UI 素材
- 推送通知的显示名称、邮件/短信模板签名是否为「投聊」
- 图标、启动图各尺寸是否齐全，有无沿用他人素材
输出清单和建议改名方案，先不要动。数据库改名必须给出迁移脚本，不能直接 rename 文件。
```

---

## 第 11 条｜私有化部署完备性

```
只读不改，检查投聊作为私有化部署产品的完备性：
- 服务器地址是否可配置（不能硬编码 IP/域名在客户端），四端是否都支持切换后端地址
- 首次部署流程：有无一键部署脚本、初始化 SQL、默认管理员账号（默认密码是否强制修改）
- 有无后台管理界面（用户管理、内容审核、封禁、数据统计），还是只能改数据库
- 授权/许可控制（如果按部署卖）、用户数上限逻辑
- 数据导出与迁移能力（客户要带走数据时）
- 部署文档是否完整：依赖版本、端口、证书、备份恢复步骤
```

---

## 第 12 条｜运维与部署

```
只审查投聊的部署配置，不改代码：
- PM2：是否配置自动重启、内存上限重启、日志切割；未处理的 Promise rejection 会不会拖垮整个服务（单体单进程风险）
- SQLite 备份：有无定时备份脚本和恢复验证
- nginx：是否配置 gzip、缓存策略、上传大小限制、WebSocket 的 proxy upgrade 头
- GitHub Actions：secret 是否泄露、部署失败是否有回滚、有无健康检查
- 是否有基础监控（进程存活、磁盘占用、连接数、错误日志告警）
- Android 版本 json 自动更新：更新包是否校验签名/哈希（防劫持）
- 日志里是否打印了密码、token、手机号等敏感信息
```

---

## 第 13 条｜补测试与压测

```
为投聊后端核心流程补自动化测试（用现有框架，没有就配 Jest + supertest）：
注册登录、发帖、点赞、关注、发消息、WebSocket 连接与鉴权、越权访问。
每个用例覆盖正常路径 + 至少两个异常路径。写完跑一遍，列出失败用例，先不要改业务代码。
另外写一个 1000 并发连接的压测脚本，验证 SQLite 和 WebSocket 在目标规模下的表现，输出连接建立耗时、消息延迟、内存占用、错误率。
```

---

## 修复阶段（每轮报告确认后使用）

```
按你报告的顺序，从第 1 个「必须修」开始。一次只改一个问题，改完贴出修改前后的代码、说明为什么这样能解决、以及我该怎么验证。我确认后再改下一个。涉及数据库结构变更时同时给出迁移脚本和回滚脚本。
```

**如果它说「已修复」但你不放心：**

```
把这个改动涉及的所有文件的修改前后代码完整贴出来，并说明这个改法在什么情况下会失效。
```

---

## 优先级提示

按这套架构的经验，最可能出问题的三处：

1. **WebSocket 握手不鉴权** → 第 2 条
2. **SQLite 未开 WAL 导致锁库** → 第 4 条
3. **推送 token 切账号不解绑导致消息串人** → 第 9 条

时间紧的话，先跑 **0 → 2 → 4 → 3 → 9**，其余按需。

---

## 进度记录

| 轮次 | 主题 | 状态 | 备注 |
|---|---|---|---|
| 0 | 摸底 | ☐ | |
| 1 | 认证与会话 | ☐ | |
| 2 | WebSocket | ☐ | |
| 3 | 越权鉴权 | ☐ | |
| 4 | SQLite | ☐ | |
| 5 | 输入安全 | ☐ | |
| 6 | 业务逻辑 | ☐ | |
| 7 | 前端健壮性 | ☐ | |
| 8 | 三端对齐 | ☐ | |
| 9 | 移动端 | ☐ | |
| 10 | 品牌命名 | ☐ | |
| 11 | 私有化部署 | ☐ | |
| 12 | 运维部署 | ☐ | |
| 13 | 测试压测 | ☐ | |
