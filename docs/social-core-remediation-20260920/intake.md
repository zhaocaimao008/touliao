# 第一阶段整改检查点

审计来源：/home/ubuntu/touliao-social-audit-20260920T013600Z/。基线d376c6a275b3af65a3edaae62d007a327c35b54f；检查点checkpoint/social-core-20260920-baseline；工作分支fix/social-core-20260920。仅开发、隔离验证，不发布。

| issue ID | 当前证据 | 涉及平台 | 修复范围 | 验收用例 |
|---|---|---|---|---|
| SOCIAL-001 | Web/Electron、Android和iOS均仅以conversationId存草稿；切换账号清消息缓存但不隔离草稿。 | Windows/Web（模块复现）；Android/iOS（CODE_ONLY） | 草稿键改为账号ID+会话ID；旧无主草稿不自动归属新账号；退出/删除账号清理明确所属数据。 | A/B同群、非同群、退出重登、独立窗口均不串号；允许保留A草稿也只能A读取。 |
| SOCIAL-002 | 个人详情会隐藏bio，群信息SELECT却直接返回每个成员bio。 | 四端 / Server | 共用按查看者过滤资料的序列化器；群成员列表只回必需公开字段。 | 陌生人、好友、共同群成员、本人四类响应一致遵循可见规则；手机与Web呈现一致。 |
| SOCIAL-003 | 直接建群/邀请校验好友和禁止直接拉群设置，未复用双方黑名单门控；拉黑不删除contacts。 | 四端 / Server | 建群与邀请统一校验双向黑名单、目标账号可用性和直接邀请设置；逐个明确失败原因。 | 双方任一方向拉黑均不可直接拉群；批量邀请部分失败不影响允许的目标；并发无重复成员。 |
| SOCIAL-004 | 消息检查双向拉黑和接收方blockUnknownMessages；call:request仅检查被叫拉黑主叫和是否已有私聊会话。 | 四端 / Server | 定义消息/通话共同基础关系策略；至少双向拉黑阻止呼叫；明确非好友来电开关或沿用现有接收策略。 | 双方拉黑、删除、重加、已有/无会话矩阵有一致错误码；服务端拒绝且无推送。 |
| SOCIAL-005 | no_private_chat持久化并广播，但创建私聊与发送服务未读取该标记；no_add_friend有独立服务端检查。 | 四端 / Server | 先固定范围（只限制从群发起，还是限制所有成员间私聊；原有好友如何处理），再将该范围落实为服务端规则，或将文案明确为仅隐藏入口。 | 按定下的好友/陌生人/多共同群规则测试HTTP及实时路径，拒绝不可仅靠按钮隐藏。 |
| SOCIAL-007 | 发起时检查目标账号；接受时检查请求归属、状态、黑名单和群规则，但未检查申请者banned。 | 四端 / Server | 接受前复查双方可用状态，在同一关系事务内决定结果并通知接收者。 | 封禁/注销/拉黑期间的旧申请不能转为好友；解除限制后的重申请规则清晰。 |
| SOCIAL-008 | 数据库写入和缓存失效存在；这些controller/service无对应用户级变更广播。好友接受、消息已读有事件，不应概括为“系统没有同步”。 | 四端 / Server | 统一用户级状态版本或失效事件；客户端收到后重新拉取，断线重连对账；避免逐字段重复实现。 | 四端双设备A改B更新；断线后恢复；迟到事件不会复活已删除关系；服务端拒绝规则不依赖UI状态。 |
| SOCIAL-010 | 动态详情assertVisible正确拒绝；notifications查询只过滤互动者拉黑，仍回moment_content和comment_content。 | 四端 / Server | 通知返回与推送构建均复用当前对象授权；无权仅返回不可用占位或删除通知，不含预览；读计数一并一致。 | 私密变更、删好友、拉黑、删除动态后列表/未读/点击安全降级。 |

SOCIAL-005范围待明确：现有文档/文案没有规定限制仅群来源陌生联系还是全部成员，且用户明确要求保留合法已有好友沟通。不得推导“同一限制群的两人全局禁聊”。先保留现有非好友创建私聊服务端拒绝及好友允许契约；不擅自扩大。

未关闭清单：SOCIAL-006/009/011/012/013/014/015/016/017/018/019/020/021/022/023均不纳入本轮开发。SOCIAL-011服务端正文保留为高风险遗留，需单独明确触发、正文/附件、缓存/备份语义，不作生产清理。

朋友圈、收藏、群通话线上关闭；代码修补不意味着启用。任何设备验证缺失必须明确记录。所有缓存、数据库、构建输出放本轮隔离目录；不复用可写共享node_modules。
